public class Save{
    // extend InputController
    // makes types FileWriter pointing to a path on my hard drive and a BufferWriter
    // throw exception if file is not found
    // History class? we need a way to save the history so that we can go through it and write it to the file
    // correct and incorrect history? -> if someone inputs the wrong thing
}
